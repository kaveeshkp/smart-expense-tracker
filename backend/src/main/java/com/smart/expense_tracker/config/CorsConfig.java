package com.smart.expense_tracker.config;

/**
 * CORS is configured in SecurityConfig.java using the Spring Security filter chain.
 * This approach is the recommended way to configure CORS in Spring Boot applications
 * with Spring Security enabled.
 */
public class CorsConfig {
}